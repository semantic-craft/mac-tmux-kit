# XcodeGen generates this from project.yml — regenerate with `xcodegen generate`.
*.xcodeproj/

# Xcode / SwiftPM
.DS_Store
DerivedData/
build/
.build/
.swiftpm/
*.xcuserstate
xcuserdata/

# Secrets / local config
*.env
.env

# Local-only Claude Code overrides (plugin scoping)
.claude/settings.local.json
